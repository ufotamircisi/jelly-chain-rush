import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lumisoft.jellychainrush',
  appName: 'Jelly Chain Rush',
  webDir: 'dist'
};

export default config;
